workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    environment:
      vars:
        GRADLE_OPTS: "-Xmx2048m"
    scripts:
      - name: Build APK
        script: |
          ./gradlew clean assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/*.apk
